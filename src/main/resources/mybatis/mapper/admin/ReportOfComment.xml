<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//matis.org//DTO Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.doneasy.don.repository.admin.project.IReportOfCommentRepository">
    <select id="findByCommentOfProjectId">
        select count(id) from reportofcomment where commentofproject_id=#{id}
    </select>
</mapper>