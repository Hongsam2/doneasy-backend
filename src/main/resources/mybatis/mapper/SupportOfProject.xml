<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//matis.org//DTO Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.doneasy.don.repository.project.SupportOfProjectRepository">
    <select id="findByMemberId" resultType="integer">
        select count(id) from supportofproject where member_id=#{member_id}
    </select>
</mapper>